apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Namespaces and infrastructure secrets come from ../base (reconciled first).
# Custom resources that need CRDs installed here go in ../configs.
resources:
  - cert-manager.yaml
  - metallb.yaml
  - openebs.yaml
  - traefik.yaml
  - security-headers-middleware.yaml
  - cloudnative-pg.yaml
  - renovate.yaml
