apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: perforce
resources:
  - namespace.yaml
  - pull.secret.yaml     # docker-registry cred for registry.aquilla.dev (SOPS)
#  - secret.yaml          # SOPS-encrypted (copy from secret.example.yaml)
  - statefulset.yaml
  - service.yaml
  - ingressroutetcp.yaml
#  - backup.yaml
