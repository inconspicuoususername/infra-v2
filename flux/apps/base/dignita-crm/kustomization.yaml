apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: dignita-crm
resources:
  - namespace.yaml
  - pull.secret.yaml     # docker-registry cred for registry.aquilla.dev (SOPS)
  - secret.yaml          # web env + postgres password (SOPS)
  - postgres.yaml
  - deployment.yaml
