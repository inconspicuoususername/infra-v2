---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: abrabus-tls
  namespace: abrabus
spec:
  secretName: abrabus-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - abrabus.aquilla.dev
