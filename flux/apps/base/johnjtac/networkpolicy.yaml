---
# Same pattern as authentik/networkpolicy.yaml: keep this DB reachable only from
# johnjtac's own pods + the CNPG operator, not from every other app namespace.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: jtac-db-ingress
  namespace: johnjtac
spec:
  podSelector:
    matchLabels:
      cnpg.io/cluster: jtac-db
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector: {}
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: cnpg-system
