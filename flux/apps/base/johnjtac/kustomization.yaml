apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: johnjtac
resources:
  - namespace.yaml
  - pull.secret.yaml     # docker-registry cred for registry.aquilla.dev (SOPS)
  - secret.yaml          # bot config + tokens (SOPS)
  - postgres.yaml
  - pvc.yaml
  - deployment.yaml
